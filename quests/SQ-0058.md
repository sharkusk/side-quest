---
title: Machine-local absolute-path hook shims are incompatible with tracked/shared hooks; evaluate PATH-relative shims (design B)
status: done
type: feature
priority: high
created: 2026-07-04T16:09:17Z
completed: 2026-07-06T04:30:54Z
commits:
  - 44ac829ce822be545b21c3bfe15ecb86e45ab0bd
  - 9fa4720f00db8994f2c84b28005e2b032c37cd97
  - 367524adbe357d1158eedb4461cbf43861329471
  - 734756d3ed0a506aa299c510be4c9c99c44f53cf
  - b7b88713579837c26e63a283742ec1c82e8267f8
  - 5a138e1e7c8f42fe66f37a55493bb51e6423e747
context: |-
  branch: main
  head: 81904d7
  cwd: /Volumes/Videos/Source/side-quest

  Asked during hook dogfooding: install-hooks writes machine-local shims (hardcoded absolute paths) into the resolved hooks dir but never touches .gitignore — fine for the default .git/hooks (never tracked), but for a tracked core.hooksPath (Husky/monorepo/side-quest's own .githooks) the shims land tracked and the user must hand-add ignore entries; this is the general form of SQ-0057, whose one-line fix was applied only to side-quest's own repo.
tags:
  area: packaging
  launch: post-0.1.0
  source: dogfood
---

--- note 2026-07-04T16:15:16Z ---

Cross-project evidence (2026-07-04): the .gitignore shim block is HAND-maintained — side-quest writes no .gitignore at all. The user's OTHER project already carries the full four-entry block including /.githooks/pre-push and the "these four" comment, copied from side-quest's own block and manually corrected, while side-quest's own repo still had the three-entry SQ-0057 bug until today. So the dogfood repo lagged the user's manual fix, and the user has been patching this per-project by hand each time a shim appears. The hand-written marker-style comment block is effectively a prototype of the feature: install-hooks should write/refresh a marker-guarded, version-aware gitignore block listing its shims whenever the resolved hooks dir is inside the work tree (tracked hooksPath). Scope = the FULL auto-managed block (warning-only is insufficient — user is still copy-pasting). Since the user habitually uses a tracked .githooks layout, this is their normal case, not an edge case → recommend launch=0.1.0.

--- note 2026-07-04T16:24:07Z ---

Root-cause / design fork (from hooks.go:45-46): the shims hardcode the ABSOLUTE binary path deliberately — "so the hooks always run the exact side-quest that installed them (no PATH reliance)" (robust when git is launched by a GUI client or cron that doesn't inherit shell PATH). That absolute path is the ONLY reason the shims are machine-local and thus leak in a tracked core.hooksPath. So there are two coherent designs, and SQ-0058 should pick one, not assume (A):
  A (today): absolute-path shims → machine-local → auto-manage a marker-guarded .gitignore block (this quest's original scope).
  B (alternative): PATH-relative shims (bare `side-quest ...`) → byte-identical across machines → can be TRACKED/committed alongside the repo's own hooks (e.g. the pre-commit doc-guard) → NO gitignore needed at all. Cost: breaks if git runs without side-quest on PATH (GUI git clients, cron).
Decision hinges on whether GUI/cron git invocation is a supported case. If not, B dissolves the problem instead of managing it. Revisit before implementing A.

--- note 2026-07-04T16:36:17Z ---

DECISIVE against option A — composing into a TRACKED hook (verified in installOneHook, hooks.go:197-232): when a repo already has an existing post-commit (or any hook) under a POSIX shell, install-hooks APPENDS its marker block — with the absolute machine-local path — into that file (hookComposed). If the hook is tracked, .gitignore CANNOT help (you can't ignore a modification to a tracked file). The user is then forced to either commit the absolute path (pollutes the shared hook, breaks it for every teammate whose binary path differs) or keep a permanently dirty tree (one `git add -A` from leaking it; a stash/checkout can silently wipe the sq block and disable linking). Non-sh hooks are skipped (hookSkipped) — user integrates by hand. Conclusion: SQ-0058 option A (auto-gitignore) covers only the NEW-separate-shim case; it does nothing for the composed-into-tracked-hook case. Only design B (PATH-relative shims) makes the appended line committable into a shared hook. This makes B the correct direction, not merely the tidier one. Consider splitting the composed-tracked-hook corruption into its own bug if we keep A for any reason.

--- note 2026-07-04T17:07:56Z ---

Design B chosen: PATH-relative shims with graceful degradation. Each shim wraps its `side-quest …` call in an `if command -v side-quest >/dev/null 2>&1; then … else warn >&2; fi` guard (if/else, NOT early `exit`, so the block is compose-safe when other hook content follows). commit-msg keeps NO `|| true` so a real require_quest reject still blocks; the else branch (binary missing) exits 0 so a missing binary never blocks a commit. Byte-identical shims ⇒ committable into a tracked hooks dir ⇒ NO auto-gitignore needed (dissolves A and retires SQ-0057). Removes the os.Executable() lookup + shimQuotedPath (incl. its Windows backslash handling) as now-dead.\n\nASSUMPTION TO VERIFY before relying on the plugin flow: the Claude Code plugin puts side-quest on the AGENT's PATH (plugin.md:24-26), so agent-run `git commit` should let the hook's `command -v side-quest` resolve. But if that PATH injection reaches only the MCP server launch and NOT Bash-tool subprocesses, agent-run git would ALSO not find the binary and graceful degradation would SILENTLY MASK it (hooks appear installed, never link). Verification test: plugin-only install (no ~/go/bin side-quest), have the agent make a commit, confirm the post-commit link actually fired. If it doesn't, that's a separate plugin-PATH-plumbing finding and the skip-warning must be discoverable rather than swallowed.

--- note 2026-07-04T17:42:34Z ---

IMPLEMENTED + merged to main (fast-forward, 2026-07-04). Commits: 9fa4720 (guardedShim + shims table, removed os.Executable/shimQuotedPath), 367524a (behavioral tests: blocking/skip/compose-safety/migration), 734756d + b7b8871 (docs), 44ac829 (spec+plan). Subagent-driven: each task reviewed clean (spec + quality); final whole-branch review on opus = READY TO MERGE, no blocking findings. Full suite green. SQ-0057 (.gitignore pre-push) closed alongside in 03dc621.\n\nQuest kept OPEN for the one manual gate the plan flagged: verify the Claude Code plugin's PATH actually reaches agent-run git (plugin-only install, no ~/go/bin side-quest, agent commits, confirm post-commit link fired). If it does NOT, file a separate plugin-PATH-plumbing bug and make the skip-warning discoverable. Also pending (post-plan step 1): `make install` + `side-quest install-hooks` in THIS repo to refresh its own hooks from absolute-path → portable format (the running binary still predates this change).

--- note 2026-07-04T17:51:40Z ---

Post-plan step 1 DONE (2026-07-04): `make dev` rebuilt HEAD (bdd37e3) and refreshed this repo's own .githooks — all four shims went v81904d7 → vbdd37e3, now the portable `command -v side-quest` format. Verified by inspection: post-commit uses `side-quest link HEAD || true`; commit-msg has NO `|| true` (blocking preserved); zero absolute paths in any shim; git tree stays clean (shims gitignored per SQ-0057). Only remaining gate = the plugin-PATH E2E check (needs a plugin-only environment; user to run).

--- note 2026-07-05T15:26:59Z ---

Local half of the plugin-PATH gate verified (2026-07-05, isolated scratch repo, binary 84938c4). Portable post-commit/commit-msg shims: (1) graceful degradation confirmed — with side-quest stripped from PATH, the commit succeeds (exit 0, not blocked); (2) skip is DISCOVERABLE, not silent — each hook prints 'side-quest: not on PATH — skipping (add it to PATH; see install-hooks)' to stderr, visible in the git/commit output; (3) baseline linking works when the binary is present. Conclusion: the ledger's 'silently mask' worry is resolved — if the plugin PATH does NOT reach agent-run Bash git, the agent sees a loud warning on every commit rather than silence. STILL OPEN: the actual plugin-only E2E (does the Claude Code plugin put side-quest on the agent's Bash PATH?) needs a real plugin session — probe: 'command -v side-quest' in the agent Bash tool with NO ~/go/bin copy; resolves=PASS, not-found=file plugin-PATH-plumbing bug (fix = cli_install launcher).

--- note 2026-07-05T15:35:26Z ---

DEFERRED to post-release (2026-07-05): re-tagged launch=0.1.0 -> launch=post-0.1.0. Rationale: the remaining plugin-PATH E2E gate is blocked by the chicken-and-egg that the plugin auto-provisions its binary by DOWNLOADING a release (bin/side-quest step 3), and there is no release yet (SQ-0055). On a fresh machine the plugin can't provision, so onboard fails. The documented offline fallback (install the binary yourself so the plugin uses it from PATH, bin/side-quest step 2) would INVALIDATE this test — it puts side-quest on the user PATH, so the hook resolves that, not the plugin's binary. Options to test properly once unblocked: (A) after SQ-0055 ships, the plugin provisions normally on a fresh machine -> run the probe; or (B) manually seed the plugin cache dir ($CLAUDE_PLUGIN_DATA/bin/side-quest-$VERSION, or ${XDG_CACHE_HOME:-~/.cache}/side-quest/bin/side-quest-$VERSION) with a locally-built binary named for VERSION so bin/side-quest step 1 uses it WITHOUT anything on user PATH. Decision: don't block 0.1.0 on this; verify post-release and ship a minor (0.1.1) if the plugin PATH turns out not to reach agent-run Bash git. Local half already verified (degradation is discoverable, not silent), so a failure here surfaces loudly rather than silently dropping links.

--- note 2026-07-05T21:05:33Z ---

Doc evidence resolving the open gate (2026-07-05, gathered while debugging SQ-0080 with the claude-code-guide agent against current Claude Code docs): the plugins-reference states a plugin's bin/ executables are "added to the Bash tool's PATH... invokable as bare commands in any Bash tool call while the plugin is enabled." That is a strong YES to this quest's core assumption — the plugin's bin/side-quest shim is on the AGENT's Bash-tool PATH, so an agent-run `git commit` (a Bash subprocess) should let the hook's `command -v side-quest` resolve it and the post-commit link fire. Not the same subsystem as SQ-0081 (that is MCP-server command resolution on Windows; this is git-hook Bash-PATH).

Closeable in a live plugin session (v0.1.3 now provisions cleanly, so the SQ-0055 chicken-and-egg blocker from the 2026-07-05 deferral note is gone). Two-step E2E probe: (1) in the plugin session's Bash tool, with NO ~/go/bin side-quest, run `command -v side-quest` — expect it resolves to the plugin's bin/side-quest shim; (2) have the agent make a commit against a current quest and confirm the post-commit hook recorded the link. Both pass => close SQ-0058. If (1) does not resolve => the graceful-degradation warning fires loudly (already verified discoverable, not silent), so file a separate plugin-PATH-plumbing bug rather than losing links.

--- note 2026-07-06T04:30:53Z ---

PIVOT + CLOSE (2026-07-06, user decision option 1). SQ-0089 this session deleted the plugin's bin/ (bin/side-quest, .cmd, launch.js), so the plugin no longer puts side-quest on the agent's Bash-tool PATH — the open verification gate's premise (command -v side-quest resolving to the plugin's bin/side-quest shim) is now obsolete. The portable-shim implementation (design B) is done, merged, and its graceful degradation is verified. Post-SQ-0089, agent-run AND terminal git commits resolve side-quest only via install-cli's launcher on the user PATH (which the Claude Bash tool inherits); onboard nudges install-cli. Decision: accept install-cli as the PATH mechanism rather than re-adding a Bash-PATH shim. Documented by correcting docs/plugin.md's now-false 'plugin puts the binary on the agent's PATH' claim. Closing.
