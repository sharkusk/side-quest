---
title: 'Windows .cmd provisioning silently fails: ASSET is set and used inside the same parenthesized if-block, so %ASSET% expands empty at parse time → download URL loses its filename → nothing provisions → MCP -32000'
status: open
type: bug
priority: low
created: 2026-07-05T22:00:05Z
commits:
  - b684a774a7dce69c1fea3bd175fd9bed052f982e
context: |-
  branch: main
  head: 34a0146
  cwd: /Volumes/Videos/Source/side-quest

  Root-caused the Windows -32000 after v0.1.4: user reports the plugin data dir is completely empty. bin/side-quest.cmd sets ASSET inside the `if not VERSION==dev (...)` block and references %ASSET% in the same block; cmd expands %VAR% in a block at parse time (before the set runs), so the download URL is malformed and the binary never downloads.
tags:
  area: packaging
---

--- note 2026-07-06T01:54:51Z ---

Superseded by SQ-0089 (v0.1.6): bin/side-quest.cmd was DELETED. Provisioning moved to scripts/provision.ps1 (PowerShell, no batch parse-time %VAR% footgun), so the empty-%ASSET% class of bug cannot recur. Closing done.
