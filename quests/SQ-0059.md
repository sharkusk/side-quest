---
title: Move the embedded AGENTS.md source out of repo root (into internal/guidance) so it isn't mistaken for dogfood config
status: open
type: feature
priority: low
created: 2026-07-04T16:17:38Z
commits:
  - e0819190ca8f0cd40b26b94d10497899015ae322
context: |-
  branch: main
  head: 81904d7
  cwd: /Volumes/Videos/Source/side-quest

  During guidance dogfooding, running onboard --agents-md inside side-quest's own repo wrapped and corrupted the //go:embed AGENTS.md source, because its root placement makes it read as the repo's own reinforcement file rather than the template the binary ships to non-Claude users.
tags:
  area: packaging
  source: dogfood
---

--- note 2026-07-04T16:17:49Z ---

Implementation sketch (TDD-friendly; guard test first):
1. NEW guard test in internal/guidance: assert the embedded agents-guidance string contains NO ">>> side-quest >>>" marker (the markers are added at runtime for consumer files; the embed source must stay unwrapped). This is the real "don't get mixed up again" fix regardless of location.
2. Move AGENTS.md -> internal/guidance/agents.md (beside core.md).
3. Fold the embed into internal/guidance/guidance.go (//go:embed agents.md var AgentsMd string); delete root assets.go (confirm nothing else lives in root package sidequest).
4. Repoint consumer: cmd/side-quest/onboard.go uses sidequest.AgentsGuidance -> guidance.AgentsMd.
5. Repoint the drift test path: internal/packaging/manifests_test.go currently reads root AGENTS.md.
6. Fix doc links: README.md:214 ([AGENTS.md](AGENTS.md)) and docs/manual-setup.md:85 (../AGENTS.md) — point to the new path or reword to "run side-quest agents-md".
7. Tradeoff accepted: lose root-level discoverability of the canonical example; side-quest's repo having no root AGENTS.md is itself an honest signal that it's a Claude-developed project (reinforcement via the skill). Verify: go test ./... green, side-quest agents-md still prints the wrapped block.
